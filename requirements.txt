# Pinned to the versions the suite was last verified against, so a rebuild
# next month installs the same code as today. Bump deliberately: change a
# pin, run the tests, commit both together.
fastapi==0.140.0
uvicorn[standard]==0.51.0
sqlalchemy==2.0.51
alembic==1.18.5
pydantic[email]==2.13.4
python-dotenv==1.2.2
bcrypt==5.0.0
PyJWT==2.13.0
python-multipart==0.0.32
httpx==0.28.1
slowapi==0.1.10
psycopg2-binary==2.9.12
pytest==9.1.1
pytest-cov==7.1.0
ruff==0.16.0
